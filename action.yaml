name: "labeler"
description: "A GitHub Action to label pull requests based on their content."

inputs:
  github_token:
    description: "GitHub token to authenticate the action."
    required: true

runs:
  using: "node16"
  main: "dist/index.js"